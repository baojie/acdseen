# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

- **预览窗格**（`preview.py`）：浏览器左下方显示当前选中图片的大图预览
  - 复刻原版 ACDSee 1.x 的 Preview Pane（Mac 版 1.5.1 默认就在左下角）
  - 单线程解码 + generation 作废；解码目标跟着窗格大小走，resize 用单发 QTimer 防抖重载
  - 选中项 / 切目录 / 删除后自动同步；看图时暂停解码让路，退出看图后恢复
  - 菜单「查看 → 预览窗格」可开关，可见性与左列分割条位置随 `QSettings` 持久化

- **缩略图右键「幻灯演示」**：从右键点中的那张开始全屏幻灯片，
  和菜单栏的 `Ctrl+S`（从第一张开始）互补
- **幻灯片间隔可设任意秒数**：看图器按 `D` 或右键「幻灯间隔…」弹对话框，范围 0–3600 秒
  - `0` 秒 = 尽快，上一张解完就立刻翻，不等墙钟
  - `[` / `]` 的档位加上 `0` 和 `0.5`
- **缩放模式「缩放到显示框」**（`Z`），并**设为默认**：单页看图和幻灯放映都占满窗口，
  小图也放大到贴住显示框，长宽比不变、不裁切
  - 原有的「适应窗口」（`*`）保留 ACDSee 原版行为：小图不放大
  - 缩放模式现在跨图保持，手动缩放后翻页会回到你选的模式而不是一律退回适应窗口
  - 2 倍以内的放大改用平滑插值（原来一律最近邻，放大小图会糊成马赛克）；
    超过 2 倍仍用最近邻，那时是在看像素
- **幻灯片乱序**：看图器按 `R` 或右键「乱序」切换，跑完一轮自动重洗；
  关掉时还原原始顺序，当前这张始终留在原地

- **浏览器列表模式**（`F8` / `Ctrl+1` `Ctrl+2`）：`QTreeView` 详细列表，
  列为 名称 / 尺寸 / 大小 / 类型 / 修改日期
  - **表头可点击排序**：点一次按该列排序，再点一次翻转正倒序，换一列回到正序；
    列宽可拖、列序可调，排序箭头与菜单「排序」双向同步（随机排序不对应任何一列，收起箭头）
  - 模型改成多列的 `QAbstractTableModel`，两个视图共用它和同一个选择模型，
    切换视图时缩略图缓存和选中项都保留；视图模式随 `QSettings` 持久化
- **排序选项补齐**：在名称 / 文件大小 / 类型 / 修改日期之外，新增
  **像素总数**、**宽度**、**高度**、**随机**，都可叠加倒序
  - 尺寸类排序要读每个文件的图片头，按 路径 + mtime 缓存，滚动和刷新不会反复读
  - 随机排序用固定 seed，删张图触发的刷新不会把整个网格重洗；再点一次「随机」才换新牌

### 变更

- **按功能拆分超过 500 行的模块**，行为不变，测试一条没改：
  - `browser.py` 829 → 292 行，分出 `thumbmodel.py` / `viewpanes.py` / `menus.py` /
    `fileops.py` / `viewhost.py` / `helptext.py`
  - `viewer.py` 593 → 413 行，分出 `slideshow.py` / `render.py`
  - 用 mixin 而非独立类：属性路径不变，与宿主的真实耦合也不用伪装成回调
- **看图器不再另开窗**：改成主窗口 `QStackedWidget` 的一页，屏幕上仍然只剩那张图
  - `Viewer` 不再决定自己的去留，改发 `exit_view` 信号；独立启动（`acdseen photo.jpg`）行为不变
  - 进入看图时禁用浏览器的 `Del` / `Enter` / `F5` 等快捷键，否则会抢在看图器的按键处理之前触发

### 修复

- **点击目录树上的软链接目录会跳到别的目录**：`set_directory()` 用了 `Path.resolve()`，
  它会穿透软链接拿到真实路径，再拿这个路径去移动树的选中行，视觉上就是"点 A 跳到 B"。
  改为只做词法规范化（`os.path.abspath` + `expanduser`）。
- 同步树选中时 `blockSignals()` 拦错了对象（`currentChanged` 连在 `selectionModel` 上，
  不是 `QTreeView` 上），导致 `set_directory()` 被递归重入一次。

## [0.1.0] - 2026-08-02

初始版本：ACDSee 1.2x 复刻的核心体验已就绪。

### 新增

- **浏览器**（`browser.py`）：左侧目录树 + 右侧缩略图，只看一个目录、不递归不建库
  - 缩略图异步加载，按需请求，滚动即见
  - 内建文件操作：重命名（F2）、删除（Del）、复制/剪切/粘贴、复制到…/移动到…
  - 排序支持名称 / 大小 / 类型 / 修改日期，名称走自然排序（IMG_2 排在 IMG_10 前）
  - 缩略图大小可调（`Ctrl++` / `Ctrl+-`，6 档），目录树可隐藏（F9）
  - 状态栏显示图片总数、选中数、当前文件尺寸/大小/时间
  - 窗口几何、分割条位置、缩略图大小、排序方式、上次目录均用 `QSettings` 持久化
- **全屏看图器**（`viewer.py`）：打开即见图，翻页无延迟
  - 两段式解码：先上 1024px 预览（JPEG DCT 缩放，快 4-8 倍），后台无缝换全尺寸
  - 预读邻居 ±2 张进 LRU 缓存，翻页缓存命中即整帧显示
  - 缩放档位 17 档（0.05×–16×），以鼠标为锚点缩放；拖拽即自动切自由缩放
  - 幻灯片可调间隔（1s–60s），解码未完成时不跳帧
  - OSD 叠层显示页码/文件名/尺寸/大小/缩放率，可开关（I）；操作反馈用瞬时闪现
  - 删除图片后自动收起列表，并同步浏览器选中
- **解码层**（`loader.py`）
  - 缩略图独立线程池 + 磁盘缓存（key = 路径 + mtime + 尺寸），切目录用 generation 作废在飞任务
  - 全图加载用 token 机制丢弃过期任务；全尺寸 LRU 缓存 8 张
  - 尊重 EXIF 方向（`setAutoTransform`）
  - 可选 Pillow 兜底 PCX / PCD / PSD 等 Qt 不认的老格式；主线程预热避免多线程并发 import 炸掉
- **启动**（`main.py`）：`python -m acdseen`、指定目录、单张图直接进全屏三种用法，记住上次目录
- **配置文件**（`config.py`）：集中放缩略图尺寸、缩放步进、幻灯片间隔、预读张数等"手感"参数

### 支持格式

- Qt 内置：BMP、GIF、JPEG、PNG、TGA、TIFF、WebP、PBM/PGM/PPM、XBM、XPM、ICO、SVG
- Pillow 兜底：PCX、PCD、PSD、JP2、AVIF、HEIC
